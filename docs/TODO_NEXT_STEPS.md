# Nächste Schritte (kurz)

1. Integrationstests für REST-Endpunkte (`/cars`) hinzufügen.
2. Automatische Datenmigration beim Start (optional).
3. Unit-Tests für `service`-Logik schreiben.
4. CI/CD: einfacher GitHub Action Workflow für `go test` und Lint.
5. Kurzvideo / Screenshot-Dokumentation für Professor erzeugen.
